# This is a secure end to end encrypted messaging application consisting of three projects

1. A web front end written in React using Vite. This should use shadcn for UI components.
2. A backend writting in ASP.Net core which provides key exchange, database management and key exchange features. This uses Entity Framework and Postgres or sqlite (depending on user preference).
3. A beautiful cross platform mobile application written using React Native.
